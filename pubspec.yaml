name: lideatech_pharmacy_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  go_router: ^13.2.0
  dio: ^5.4.0
  get_it: ^7.6.4
  injectable: ^2.3.2
  freezed_annotation: ^2.4.1
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  geolocator: ^10.1.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.8
  freezed: ^2.4.7
  injectable_generator: ^2.6.1

flutter:
  uses-material-design: true